#!/usr/bin/env sh

set -eu

if [ "${BIR_HESAB_SKIP_PUSH_CHECKS:-0}" = "1" ]; then
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root" || exit 0

printf '%s\n' "[pre-push] Running syntax checks..."
node scripts/check-syntax.js
